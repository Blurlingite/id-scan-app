{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "jsx": "react", // For React JSX
    "module": "esnext", // Ensure module system is compatible with JSX transformations
    "strict": true
  }
}
